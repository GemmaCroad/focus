<%= form_for @photo, multipart: do |f| %>

	<p>
		<%= label_tag "Animal Name: " %>
		<%= text_field_tag :name %>
	</p>	

	<p>
		<%= label_tag "Animal Image: " %>
		<%= cl_image_upload_tag :file %>
	</p>

	<%= submit_tag "Add Animal" %>

<% end %>	